{
  "buildCommand": "cd artifacts/cleantex-website && pnpm install && pnpm run build",
  "outputDirectory": "artifacts/cleantex-website/dist",
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api/$1" }]
}
